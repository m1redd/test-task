# No Try-Catch Rule

**NEVER write `try...catch` blocks. `try...finally` is OK.**

- Let errors propagate up — do NOT swallow them
- NEVER write `try...catch` EXCEPT in rare explicitly justified cases
