# Git and Version Control

**CRITICAL: NEVER EVER run `git add` or `git commit` commands!**

- NEVER use `git add` — interferes with the developer's review process
- NEVER use `git commit` — bypasses required workflow validation
- DO make code changes using Edit/Write/Read tools
- The developer handles git operations for reviewing changes
